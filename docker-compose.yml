services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: vanopt
      POSTGRES_USER: vanopt
      POSTGRES_PASSWORD: vanopt
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: